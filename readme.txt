# Real Estate Search Page – Houzeo Assignment

This project is a responsive real estate search page built with HTML, CSS, JavaScript (without Vue.js), structured in a standard PHP folder format.

---

## 🚀 Getting Started (Run Locally)

### 🛠️ Option 1: Run using XAMPP (PHP Environment)

1. Open **XAMPP**.
2. Start the **Apache** server.
3. Place the project folder inside the `htdocs` directory.
4. Access the project in your browser via:  
   `http://localhost/your-folder-name`

---

### 🧪 Option 2: Run with Localhost (No Backend)

If you only want to run the frontend locally (no PHP):

1. You can directly open the `index.html` in your browser.  
   OR  
2. Use any local development server (e.g., Live Server in VS Code or serve on port `3000`).

---

## 💅 Development & CSS Changes

If you modify the CSS (SCSS) files:

1. Install dependencies:
   ```bash
   npm install

Then run Grunt:
    npx grunt

If you encounter an error related to Grunt, make sure it's installed globally:
    npm install -g grunt-cli